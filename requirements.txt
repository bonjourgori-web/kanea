# ── Dashboard UI ────────────────────────────────────────────────────────────────
streamlit>=1.30.0
streamlit-folium>=0.18.0

# ── Data ────────────────────────────────────────────────────────────────────────
pandas>=2.0.0
numpy>=1.24.0
scipy>=1.11.0
statsmodels>=0.14.0
pillow>=10.0.0

# ── Visualisation ───────────────────────────────────────────────────────────────
plotly>=5.18.0
folium>=0.16.0

# ── Machine Learning ────────────────────────────────────────────────────────────
scikit-learn>=1.3.0
xgboost>=2.0.0
lightgbm>=4.0.0
shap>=0.44.0
joblib>=1.3.0

# ── ONNX Runtime (malaria_scan_ai — remplace torch sur Streamlit Cloud) ─────────
onnxruntime>=1.17.0

# ── BioID AI — Rapports PDF & Visualisations ────────────────────────────────────
reportlab>=4.1.0
qrcode[pil]>=7.4.2
matplotlib>=3.8.0
seaborn>=0.13.0

# ── Utilitaires ──────────────────────────────────────────────────────────────────
requests>=2.31.0
tqdm>=4.66.0

# ── Deep Learning (optionnel — lazy import si modèles disponibles) ───────────────
# torch/torchvision : trop lourds (~700 MB) pour Streamlit Cloud free tier.
# Installation locale : pip install torch torchvision --index-url https://download.pytorch.org/whl/cpu
# shap et lightgbm maintenant dans les dépendances principales (NutriTrack AI)
